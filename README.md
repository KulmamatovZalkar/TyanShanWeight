# TyanShanWeight

Приложение для автоматизации взвешивания грузовиков на заводе.

## Возможности

- ✅ Чтение веса с электронных весов по RS-232 (COM-порт)
- ✅ Антидребезг (стабилизация показаний)
- ✅ Фиксация тары и брутто
- ✅ Автоматический расчет нетто
- ✅ Сохранение в локальную БД (SQLite)
- ✅ Отправка данных на webhook API
- ✅ Современный темный интерфейс
- ✅ Горячие клавиши

## Установка

### Вариант 1: Из исходников

1. Установите Python 3.11+
2. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   venv\Scripts\activate
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Запустите:
   ```bash
   python main.py
   ```
   Или используйте `run.bat`

### Вариант 2: Сборка EXE

Запустите `build.bat` для создания автономного EXE файла.
Результат будет в `dist\TyanShanWeight\`

## Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| F1 | Зафиксировать тару |
| F2 | Зафиксировать брутто |
| F3 | Сохранить |
| Esc | Сброс |
| F5 | Настройки |

## Настройка весов

1. Откройте **Настройки** (F5)
2. Выберите COM-порт весов
3. Установите параметры связи (обычно 9600, 8N1)
4. Нажмите **Тест подключения**
5. Сохраните настройки

## Формат данных

Приложение поддерживает различные форматы данных весов:
- `+  1234.5 kg`
- `ST,GS,+001234.5 kg` (CAS)
- `1234.5`

Настроить паттерн можно в разделе **Дополнительно**.

## API

Данные отправляются POST-запросом в формате JSON:

```json
{
  "datetime": "2026-01-19T16:50:00",
  "car_number": "А123БВ",
  "tara": 5000.0,
  "brutto": 15000.0,
  "netto": 10000.0,
  "fio": "Иванов И.И.",
  "fraction": "Щебень 5-20"
}
```

## Структура проекта

```
TyanShanWeight/
├── main.py              # Точка входа
├── requirements.txt     # Зависимости
├── config.json          # Конфигурация (создается автоматически)
├── weighings.db         # База данных (создается автоматически)
├── src/
│   ├── gui/             # Интерфейс
│   ├── core/            # Бизнес-логика
│   ├── data/            # Работа с данными
│   ├── hardware/        # Работа с оборудованием
│   └── utils/           # Утилиты
├── logs/                # Логи
└── build/               # Файлы сборки
```

## Лицензия

Проприетарное ПО. Все права защищены.
